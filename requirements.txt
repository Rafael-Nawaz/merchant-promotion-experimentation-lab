# Merchant Promotion Experimentation Lab - Python dependencies
pandas>=2.0
numpy>=1.24
statsmodels>=0.14
scipy>=1.10
openpyxl>=3.1
xlsxwriter>=3.1

# Optional: only needed for src/validate_analytics.py (runs the SQL view layer
# in-process to cross-check it against the Python analysis without a Postgres server)
duckdb>=1.0
